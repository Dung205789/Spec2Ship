# Evaluation / dataset tooling (kept separate from backend deps)
httpx>=0.27
datasets>=2.18,<3
swebench==4.1.0
tqdm>=4.66,<5

# Optional: training / HF local inference
torch>=2.2,<3
transformers>=4.40,<5
peft>=0.11,<1
accelerate>=0.29,<1
sentencepiece>=0.2,<1
