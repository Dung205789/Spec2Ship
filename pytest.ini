[pytest]
markers =
    smoke: fast API tests, no pipeline needed (~10s)
    rules: rules patcher tests (~2-3 min, requires docker compose up)
    ollama: ollama LLM tests (~5-10 min, requires PATCHER_MODE=ollama)
addopts = --tb=short
